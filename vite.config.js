import { defineConfig } from 'vite'

export default defineConfig({
  // './' makes asset paths relative — works for GitHub Pages subdirectory deploys.
  // If you deploy to a custom domain or GitHub user page (username.github.io),
  // change this to '/' instead.
  base: './',
})
