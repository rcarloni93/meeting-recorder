{
  "name": "meeting-recorder",
  "version": "1.0.0",
  "description": "Record meetings and auto-transcribe with speaker diarization via AssemblyAI",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  }
}
